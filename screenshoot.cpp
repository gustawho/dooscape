#include "screenshoot.h"

ScreenShoot::ScreenShoot(QWidget *parent) :
    Panel(parent)
{
    setTitle("Screen Shoot");

    // A creer : un panel qui permet de faire des captures d'écran des page web en mode avancé (mode rafale, delai, zone de capture, modifier les images, l'emplacement d'enreg, etc...)
}
